% ============================================================
% main.tex — Coronary Heart Disease Prediction Report
% Individual Coursework — Machine Learning
%
% Requirements:
%   - Max 1000 words, 5 pages
%   - Font size >= 12pt, margins >= 1 inch, 1.5 line spacing
%   - Compiled from the report/ directory:
%       pdflatex main.tex
%       bibtex main
%       pdflatex main.tex (x2)
% ============================================================

\documentclass[12pt,a4paper]{article}

% ── Page layout ───────────────────────────────────────────────────────────────
\usepackage[top=1in, bottom=1in, left=1in, right=1in]{geometry}

% ── Line spacing ──────────────────────────────────────────────────────────────
\usepackage{setspace}
\onehalfspacing

% ── PDF inclusion (submission form) ───────────────────────────────────────────
\usepackage{pdfpages}

% ── Typography & encoding ─────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}

% ── Tables & figures ──────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{array}          % extended column formatting
\usepackage{multirow}       % multirow cells in tables
\usepackage{wrapfig}        % text wraps around figures/tables
\usepackage{placeins} % use \FloatBarrier explicitly where needed

% ── Code listings ─────────────────────────────────────────────────────────────
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color[gray]{0.95},
  frame=single,
  framerule=0.4pt,
  rulecolor=\color[gray]{0.6},
  breaklines=true,
  breakatwhitespace=true,
  xleftmargin=6pt,
  xrightmargin=6pt,
  aboveskip=4pt,
  belowskip=4pt,
}
\usepackage{xcolor}

% ── Float placement defaults (prevents overflow past page bottom) ─────────────
\renewcommand{\topfraction}{0.85}       % up to 85 % of page may be top floats
\renewcommand{\bottomfraction}{0.70}    % up to 70 % of page may be bottom floats
\renewcommand{\textfraction}{0.10}      % at least 10 % of page must be text
\renewcommand{\floatpagefraction}{0.85} % float-only page must be >= 85 % full
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\textfloatsep}{10pt plus 2pt minus 4pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}

% ── Hyperlinks ────────────────────────────────────────────────────────────────
\usepackage[colorlinks=true, linkcolor=black, citecolor=blue, urlcolor=blue]{hyperref}

% ── References ────────────────────────────────────────────────────────────────
\usepackage[numbers,sort&compress]{natbib}
\usepackage{doi}            % clickable DOI hyperlinks

% ── ToC depth ─────────────────────────────────────────────────────────────────
\setcounter{tocdepth}{1}    % sections only — saves space

% ── Appendix ──────────────────────────────────────────────────────────────────
\usepackage[title]{appendix}

% =============================================================================
\begin{document}

% ── Page 1: Coursework Submission Form ───────────────────────────────────────
\includepdf[pages=1]{Group Coursework Submission Form.pdf}

% ── Table of Contents ─────────────────────────────────────────────────────────
\pagenumbering{gobble}
\tableofcontents
\newpage
\pagenumbering{arabic}
\setcounter{page}{1}

% =============================================================================
\section{Exploratory Data Analysis}
\label{sec:eda}
% =============================================================================

\begin{wraptable}{r}{0.46\linewidth}
\vspace{-6pt}
\captionsetup{width=\linewidth}
\centering
\caption{\footnotesize Descriptive statistics for all features.}
\label{tab:desc}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lrrrr@{}}
\toprule
Feature & Mean & Std & Min & Max \\
\midrule
sbp       & 138.3 & 20.5 & 101 & 218 \\
tobacco   &   3.6 &  4.6 &   0 &  31 \\
ldl       &   4.7 &  2.1 &   1 &  15 \\
adiposity &  25.4 &  7.8 &   7 &  42 \\
famhist   &   0.4 &  0.5 &   0 &   1 \\
typea     &  53.1 &  9.8 &  13 &  78 \\
obesity   &  26.0 &  4.2 &  15 &  47 \\
alcohol   &  17.0 & 24.5 &   0 & 147 \\
age       &  42.8 & 14.6 &  15 &  64 \\
\bottomrule
\end{tabular}}
\vspace{-4pt}
\end{wraptable}

The \texttt{heart-disease.csv} dataset contains 462 male patients from the
Western Cape, South Africa, described by nine clinical features and a binary
target \texttt{chd} (1\,=\,CHD present) with no missing values.
Four features exceed the $|\gamma_{1}|>1$ skewness threshold: \texttt{alcohol}
($\gamma_{1}=2.31$), \texttt{tobacco} ($\gamma_{1}=2.08$), \texttt{ldl}
($\gamma_{1}=1.31$), and \texttt{sbp} ($\gamma_{1}=1.18$), suggesting
log-transformation may improve classifier performance.
Table~\ref{tab:desc} summarises the key descriptive statistics. Four features

Of the 462 patients, 160 (34.6\%) are CHD-positive and 302 (65.4\%)
CHD-negative, giving an imbalance ratio of 1.89:1. This means raw accuracy
will be a misleading evaluation metric
\cite{rehman2025predicting, banerjee2025systematic, dutta2019efficient, borges2025logistic}
and alternative measures such as AUC and F1-score are used throughout.
The Pearson correlation heatmap (Figure~\ref{fig:corr}) reveals that
\texttt{age} ($r=0.37$) is the strongest linear predictor of CHD, followed
by \texttt{tobacco} ($r=0.30$), \texttt{famhist} ($r=0.27$), and \texttt{ldl}
($r=0.26$).

% ── Correlation heatmap ────────────────────────────────────────────────────────
\begin{wrapfigure}{r}{0.46\linewidth}
\vspace{-6pt}
\captionsetup{width=\linewidth}
\centering
\includegraphics[width=\linewidth]{../exploratory_data_analysis/eda_output/fig_correlation_heatmap.png}
\caption{\footnotesize Pearson correlation matrix.}
\label{fig:corr}
\vspace{-8pt}
\end{wrapfigure}

\texttt{alcohol} ($r=0.06$) and \texttt{obesity} ($r=0.10$) are
the weakest predictors. A moderate positive correlation between
\texttt{adiposity} and \texttt{obesity} ($r=0.72$) suggests partial
multicollinearity, motivating ridge regularisation in Section~\ref{sec:lrrp}
\cite{hassan2022effectively, ogunpola2024machine}.
Violin plots stratified by CHD class confirm that \texttt{age}, \texttt{tobacco},
and \texttt{ldl} show the clearest class-conditional separation, while
\texttt{obesity} and \texttt{typea} distributions overlap substantially.

Log\,\textsubscript{1p}-transformation of the four dynamically identified
skewed features reduces their skewness markedly (Figure~\ref{fig:logtr}):
\texttt{alcohol} from 2.31 to 0.44, \texttt{tobacco} from 2.08 to 0.53,
\texttt{ldl} from 1.31 to 0.50, and \texttt{sbp} from 1.18 to 0.48.
These transformations are applied as a preprocessing option in the
modelling stages (Sections~\ref{sec:lrrp}--\ref{sec:oc}).

% ── Feature importance paragraph (plain — fills page 1 bottom) ───────────────
Feature importance was assessed jointly using mutual information (MI) and
the ANOVA F-test \cite{elsofany2024proposed, ullah2024machine}. Using two
complementary criteria guards against misleading rankings from linear
assumptions alone. Table~\ref{tab:fi} shows the normalised scores: \texttt{age}
ranks first under both criteria, while \texttt{obesity} and \texttt{typea}
score lowest. These rankings directly inform the variable selection strategy
for the ridge logistic regression in Section~\ref{sec:lrrp}.

% ── Distribution analysis + log-transform ────────────────────────────────────
\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{../exploratory_data_analysis/eda_output/fig_log_transformed_features.png}
\caption{\footnotesize Log\textsubscript{1p}-transformation of the
four dynamically identified skewed features.}
\label{fig:logtr}
\end{figure}

% ── Table 2 wraptable + PCA (table on right, PCA text wraps left and below) ──
\begin{wraptable}{r}{0.46\linewidth}
\vspace{-6pt}
\captionsetup{width=\linewidth}
\centering
\caption{\footnotesize Normalised feature importance scores (ranked by mean).}
\label{tab:fi}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lrrr@{}}
\toprule
Feature    & MI   & ANOVA & Mean \\
\midrule
age        & 1.00 & 1.00  & 1.00 \\
tobacco    & 0.39 & 0.60  & 0.50 \\
famhist    & 0.43 & 0.48  & 0.46 \\
sbp        & 0.34 & 0.22  & 0.28 \\
ldl        & 0.00 & 0.45  & 0.22 \\
adiposity  & 0.00 & 0.41  & 0.21 \\
alcohol    & 0.16 & 0.00  & 0.08 \\
typea      & 0.07 & 0.04  & 0.06 \\
obesity    & 0.00 & 0.04  & 0.02 \\
\bottomrule
\end{tabular}}
\vspace{-4pt}
\end{wraptable}

PCA was applied to the standardised feature matrix
\cite{banerjee2025systematic}. PC1 explains 32.1\% of variance and loads
positively on \texttt{adiposity} (0.52), \texttt{age} (0.46), and
\texttt{obesity} (0.40) --- a latent \emph{metabolic risk} axis. PC2 is
driven by \texttt{alcohol} (0.54) and \texttt{tobacco} (0.46), reflecting a
\emph{lifestyle} axis. Seven components are needed to reach 90\% cumulative
variance, indicating moderate intrinsic dimensionality. The 2-D
projection (Figure~\ref{fig:pca}) shows partial but incomplete class
separation; the loadings heatmap (Figure~\ref{fig:pcaload}) confirms PC1
captures metabolic risk while PC2 captures lifestyle behaviours, motivating
non-linear classifiers in Section~\ref{sec:oc}.

% =============================================================================
\section{Logistic Regression with Ridge Penalty}
\label{sec:lrrp}
% =============================================================================

% ── PCA figures (float to next available position) ───────────────────────────
\begin{figure}[htbp]
\centering
\begin{minipage}{0.54\linewidth}
\centering
\includegraphics[width=\linewidth]{../exploratory_data_analysis/eda_output/fig_pca_scatter.png}
\caption{\footnotesize PCA 2-D projection coloured by CHD class.}
\label{fig:pca}
\end{minipage}\hfill
\begin{minipage}{0.42\linewidth}
\centering
\includegraphics[width=\linewidth]{../exploratory_data_analysis/eda_output/fig_pca_loadings_heatmap.png}
\caption{\footnotesize PCA feature loadings (PC1 \& PC2).}
\label{fig:pcaload}
\end{minipage}
\end{figure}

Logistic regression with an L2 (ridge) penalty was chosen as the primary
interpretable classifier. To understand why ridge was chosen over lasso,
it helps to think geometrically. Both penalties constrain how large the
coefficients can be, but the shape of the constraint differs: lasso uses a
diamond shape, while ridge uses a circle. Because a diamond has sharp corners
sitting on the axes, the likelihood surface almost always touches it at a
corner, which forces one or more coefficients to exactly zero. A circle has
no corners, so the likelihood surface touches it at a smooth point and every
coefficient is shrunk a little but none are eliminated. Since all eleven
features in this model are clinically motivated and EDA confirmed each
contributes signal, eliminating any arbitrarily would be inappropriate.
Ridge's circular constraint shrinks correlated coefficients jointly without
zeroing any out, which is exactly the behaviour needed given the
multicollinearity between \texttt{adiposity} and \texttt{obesity}
($r=0.72$, Section~\ref{sec:eda}) \cite{borges2025logistic, hassan2022effectively}.

\textbf{Feature engineering.}
Two multiplicative interaction terms were derived from the nine original
variables to capture joint effects that a linear model cannot represent
with base features alone.

\textit{(i) \texttt{age\_tobacco} $=$ age $\times$ tobacco.}
The \texttt{tobacco} column records cumulative lifetime consumption in kg,
not a daily rate. A young patient and an older patient may carry the same
lifetime total yet differ substantially in their cardiovascular trajectory:
the older patient has sustained arterial exposure for far longer.
Multiplying age by tobacco encodes both quantity and duration of exposure in
a single term. \textbf{Assumption:} the marginal CHD risk of additional
tobacco consumption is multiplicative with age, i.e.\ the dose--risk
relationship compounds over decades rather than remaining constant
\cite{rehman2025predicting}.

\textit{(ii) \texttt{age\_famhist} $=$ age $\times$ famhist.}
Family history is a binary genetic risk indicator. Genetic predisposition
is not static: a 60-year-old with family history represents a materially
higher immediate risk than a 30-year-old with the same flag. The
interaction allows the model to represent this compounding effect, which
ridge regularisation would otherwise suppress by shrinking the
\texttt{famhist} coefficient towards zero. \textbf{Assumption:} the
influence of family history amplifies with age rather than remaining
additive and age-independent. Because \texttt{famhist} is binary, the
term is equivalent to a piecewise age slope — positive for patients with
family history and zero otherwise.

\textbf{Limitations.} Both terms inherit the measurement limitations of
their parent variables (self-reported tobacco total; binary family history
without severity grading). The multiplicative form imposes a specific
functional shape; a flexible non-parametric model may better approximate
the true relationship.

\textbf{Preprocessing.}
\texttt{famhist} encoded as 0/1; skewed features (\texttt{sbp},
\texttt{tobacco}, \texttt{ldl}, \texttt{alcohol})
log\,\textsubscript{1p}-transformed; all eleven features standardised with
\texttt{StandardScaler} (zero mean, unit variance) prior to fitting ---
mandatory for ridge so the penalty treats all coefficients on the same scale
\cite{hassan2022effectively}.
The regularisation strength $C$ (inverse of $\lambda$) was selected by
stratified 5-fold cross-validation over $\{0.001,0.01,0.1,1,10,100\}$
maximising AUC-ROC on an 80\,\% training set.

Two logistic regression models are fitted on the same standardised training
set. (i)~An \textbf{unpenalised} model via maximum likelihood (statsmodels)
provides the classical inference table --- coefficient, standard error,
$z$-statistic, and $p$-value --- consistent with the lecture formulation
(Equation~\ref{eq:logit}). (ii)~The \textbf{ridge} model ($C=0.01$)
is used for prediction; its L2 penalty biases the coefficients towards zero,
which invalidates classical $p$-values but stabilises estimates under
multicollinearity.

\begin{equation}
\log\!\left(\frac{\Pr(Y\!=\!1\mid X)}{\Pr(Y\!=\!0\mid X)}\right)
= \beta_0 + \beta_1 X_1 + \cdots + \beta_p X_p
\label{eq:logit}
\end{equation}

\vspace{4pt}
\noindent
\begin{minipage}[t]{0.50\linewidth}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize Unpenalised LR inference table (training set,
$n=369$). $^{*}p<0.05$; $^{**}p<0.01$.}
\label{tab:inference}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lrrrr@{}}
\toprule
Feature & Coef & SE & $z$ & $p$ \\
\midrule
Intercept    & $-$0.851 & 0.138 & $-$6.19 & $<$0.001$^{**}$ \\
sbp          &    0.136 & 0.135 &    1.01 & 0.312 \\
tobacco      &    0.307 & 0.261 &    1.18 & 0.239 \\
ldl          &    0.420 & 0.148 &    2.84 & 0.005$^{**}$ \\
adiposity    & $-$0.157 & 0.268 & $-$0.59 & 0.558 \\
famhist      & $-$0.357 & 0.527 & $-$0.68 & 0.498 \\
typea        &    0.381 & 0.135 &    2.82 & 0.005$^{**}$ \\
obesity      &    0.017 & 0.211 &    0.08 & 0.938 \\
alcohol      & $-$0.050 & 0.131 & $-$0.38 & 0.704 \\
age          &    0.513 & 0.236 &    2.17 & 0.030$^{*}$ \\
age\_tobacco &    0.088 & 0.243 &    0.36 & 0.718 \\
age\_famhist &    0.825 & 0.535 &    1.54 & 0.123 \\
\bottomrule
\end{tabular}}
\end{minipage}\hfill
\begin{minipage}[t]{0.46\linewidth}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize Ridge LR coefficients ($C\!=\!0.01$),
sorted by $|\hat{\beta}|$.}
\label{tab:coef}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lr@{\quad}lr@{}}
\toprule
Feature & $\hat{\beta}$ & Feature & $\hat{\beta}$ \\
\midrule
age          &  0.212 & typea       &  0.122 \\
age\_famhist &  0.189 & sbp         &  0.079 \\
ldl          &  0.174 & adiposity   &  0.073 \\
tobacco      &  0.150 & alcohol     & $-$0.015 \\
famhist      &  0.130 & obesity     &  0.013 \\
age\_tobacco &  0.125 &             &        \\
\bottomrule
\end{tabular}}
\end{minipage}

\vspace{6pt}
The inference table (Table~\ref{tab:inference}) shows that \texttt{ldl}
($z=2.84$, $p=0.005$), \texttt{typea} ($z=2.82$, $p=0.005$), and
\texttt{age} ($z=2.17$, $p=0.030$) are statistically significant predictors
under MLE. \texttt{tobacco}, \texttt{famhist}, \texttt{obesity}, and
\texttt{alcohol} do not reach significance individually, partly due to
multicollinearity inflating their standard errors --- the precise reason
ridge is preferred for prediction. The optimal $C=0.01$ (Table~\ref{tab:coef})
implies strong regularisation: \texttt{age} retains the largest ridge
effect ($\hat{\beta}=0.212$), followed by \texttt{age\_famhist} ($0.189$)
and \texttt{ldl} ($0.174$). \texttt{obesity} and \texttt{alcohol} are
shrunk near zero, consistent with their non-significance in the MLE fit.

% ── ROC curves and confusion matrices moved to Section 3 for side-by-side comparison

% ── Table 4: minipage pair — text left, table right, full-width text below ───
\noindent
\begin{minipage}[t]{0.46\linewidth}
\raggedright
On the held-out test set ($n=93$, 20\,\%) the model achieves
AUC\,=\,0.820, F1\,=\,0.610, and accuracy\,=\,0.753
(Table~\ref{tab:perf}). CHD recall is 0.563 versus No-CHD recall of 0.871,
a consequence of the 1.89:1 class imbalance (Figures~\ref{fig:roc}--\ref{fig:oc_roc},
Tables~\ref{tab:cm}--\ref{tab:oc_cm}, Section~\ref{sec:oc}).
\end{minipage}\hfill
\begin{minipage}[t]{0.50\linewidth}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize Test-set performance of the ridge logistic
regression.}
\label{tab:perf}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lrrrr@{}}
\toprule
Model & AUC & F1 & Prec. & Rec. \\
\midrule
\shortstack[l]{Ridge LR\\($C\!=\!0.01$)} & 0.820 & 0.610 & 0.667 & 0.563 \\
\bottomrule
\end{tabular}}
\end{minipage}

\vspace{4pt}
The derived interaction features improve recall by 12.5 percentage points
relative to the nine-feature baseline. AUC of 0.820 is substantially above
the 0.5 random baseline and competitive with published benchmarks on similar
datasets \cite{rehman2025predicting, ogunpola2024machine}.

% =============================================================================
\section{Alternative Classifiers}
\label{sec:oc}
% =============================================================================

% ── CV screening: wraptable right, text wraps left and below ─────────────────
\begin{wraptable}{r}{0.46\linewidth}
\vspace{-6pt}
\captionsetup{width=\linewidth}
\centering
\caption{\footnotesize 5-fold CV screening of all classifiers
(AUC-ROC, F1, accuracy).}
\label{tab:cv}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lrrr@{}}
\toprule
Classifier & CV AUC & CV F1 & CV Acc \\
\midrule
\shortstack[l]{Decision\\Tree}     & 0.618  & 0.397 & 0.611 \\[4pt]
KNN                                & 0.659  & 0.453 & 0.673 \\[4pt]
\shortstack[l]{Gradient\\Boosting} & 0.699  & 0.475 & 0.685 \\[4pt]
\shortstack[l]{SVM\\(RBF)}         & 0.716  & 0.511 & 0.722 \\[4pt]
\textbf{\shortstack[l]{Random\\Forest}} & \textbf{0.718} & \textbf{0.491} & \textbf{0.689} \\
\bottomrule
\end{tabular}}
\vspace{-4pt}
\end{wraptable}

The PCA 2-D projection (Section~\ref{sec:eda}) showed only partial linear
class separation, motivating non-linear alternatives to ridge logistic
regression. Five classifiers from the module were evaluated: a
\textbf{Decision Tree} (interpretable baseline), \textbf{Random Forest}
(bagged ensemble), \textbf{SVM with RBF kernel} (margin-based, non-linear),
\textbf{KNN} (non-parametric, distance-based), and \textbf{Gradient
Boosting} (sequential ensemble). Preprocessing was identical to
Section~\ref{sec:lrrp}: \texttt{famhist} encoded, four skewed features
log\,\textsubscript{1p}-transformed, all features standardised.

All five were screened by stratified 5-fold cross-validation on AUC-ROC
(Table~\ref{tab:cv}), using the same 11-feature set (including
\texttt{age\_tobacco} and \texttt{age\_famhist}) as Section~\ref{sec:lrrp}.
Decision Tree performed worst (CV-AUC\,=\,0.618), confirming that a single
tree overfits without pruning. Random Forest achieved the highest CV-AUC
(0.718), marginally ahead of SVM (0.716) and Gradient Boosting (0.699).
Random Forest was therefore selected for hyperparameter tuning.

GridSearchCV with 5-fold CV optimised three hyperparameters simultaneously:
\texttt{n\_estimators} $\in\{100, 300, 500\}$,
\texttt{max\_depth} $\in\{3, 5, \text{None}\}$,
\texttt{max\_features} $\in\{\text{sqrt}, \text{log2}\}$.
The best configuration was \texttt{max\_depth=3}, \texttt{max\_features=sqrt},
\texttt{n\_estimators=500} (tuned CV-AUC\,=\,0.742). Shallow trees
(\texttt{max\_depth=3}) prevent overfitting on a dataset of 462 observations.

% ── ROC curves: Ridge LR vs Random Forest side by side ───────────────────
\begin{figure}[htbp]
\centering
\begin{minipage}{0.48\linewidth}
\centering
\includegraphics[width=\linewidth]{../logistic_regression_ridge_penalty/lrrp_output/fig_lrrp_roc_curve.png}
\caption{\footnotesize Ridge LR ($C\!=\!0.01$): AUC\,=\,0.820, test set $n=93$.}
\label{fig:roc}
\end{minipage}\hfill
\begin{minipage}{0.48\linewidth}
\centering
\includegraphics[width=\linewidth]{../other_classifiers/oc_output/fig_oc_roc_curve.png}
\caption{\footnotesize Tuned Random Forest: AUC\,=\,0.788, test set $n=93$.}
\label{fig:oc_roc}
\end{minipage}
\end{figure}

% ── Confusion matrices: Ridge LR vs Random Forest side by side ────────────
\vspace{2pt}
\noindent
\begin{minipage}[t]{0.46\linewidth}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize Ridge LR: confusion matrix (threshold\,=\,0.5).
Recall — No\,CHD\,85\%, CHD\,56\%.}
\label{tab:cm}
\vspace{2pt}
{\footnotesize
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{@{}llrr@{}}
& & \multicolumn{2}{c}{\textit{Predicted label}} \\
\cmidrule(l){3-4}
& & No\,CHD & CHD \\
\midrule
\multirow{2}{*}{\textit{True Label}} & No\,CHD & \textbf{52} & 9  \\
                                     & CHD     & 14          & \textbf{18} \\
\bottomrule
\end{tabular}}
\end{minipage}\hfill
\begin{minipage}[t]{0.46\linewidth}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize RF: confusion matrix (threshold\,=\,0.5).
Recall — No\,CHD\,87\%, CHD\,50\%.}
\label{tab:oc_cm}
\vspace{2pt}
{\footnotesize
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{@{}llrr@{}}
& & \multicolumn{2}{c}{\textit{Predicted label}} \\
\cmidrule(l){3-4}
& & No\,CHD & CHD \\
\midrule
\multirow{2}{*}{\textit{True Label}} & No\,CHD & \textbf{53} & 8  \\
                                     & CHD     & 16          & \textbf{16} \\
\bottomrule
\end{tabular}}
\end{minipage}

% ── Table 6: minipage pair — text left, table right, full-width text below ───
\vspace{4pt}
The ROC curves (Figures~\ref{fig:roc} and~\ref{fig:oc_roc}) show that Ridge LR
(AUC\,=\,0.820) outperforms the tuned Random Forest (AUC\,=\,0.788); both lie
well above the diagonal random-classifier baseline. The confusion matrices
(Tables~\ref{tab:cm} and~\ref{tab:oc_cm}) confirm that both models struggle
more with CHD recall than No-CHD recall, a direct consequence of the 1.89:1
class imbalance.

\noindent
\begin{minipage}[t]{0.44\linewidth}
\raggedright
On the held-out test set ($n=93$) the tuned Random Forest achieves
AUC\,=\,0.788, F1\,=\,0.571, and accuracy\,=\,0.742
(Table~\ref{tab:final}). Ridge LR attains a higher AUC (0.820) and
F1 (0.610), suggesting the L2-regularised model generalises better
when derived interaction features are included.
\end{minipage}\hfill
\begin{minipage}[t]{0.52\linewidth}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize Final comparison: Ridge LR vs.\ best
alternative classifier.}
\label{tab:final}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lrrrr@{}}
\toprule
Model & AUC & F1 & Prec. & Rec. \\
\midrule
\shortstack[l]{Ridge LR\\($C\!=\!0.01$)}  & \textbf{0.820} & \textbf{0.610} & \textbf{0.667} & \textbf{0.563} \\
\shortstack[l]{Random Forest\\(tuned)}    & 0.788 & 0.571 & 0.667 & 0.500 \\
\bottomrule
\end{tabular}}
\end{minipage}

\vspace{4pt}
Feature importances confirm that \texttt{age}, \texttt{age\_famhist}, and
\texttt{ldl} are the dominant predictors, consistent with EDA rankings and
ridge coefficients \cite{rehman2025predicting, ogunpola2024machine}.

\textbf{Methods considered but excluded.}
\textit{Factor Analysis} (FA) is an unsupervised latent-variable model
designed for structure discovery, not prediction; PCA already fulfils this
exploratory role in Section~\ref{sec:eda}. Critically, FA assumes
multivariate normality across all features --- violated by the binary
\texttt{famhist} and the four right-skewed clinical variables.

The generative classifiers in the module lecture notes (Naive Bayes, LDA,
QDA, and Fisher's DA) each impose assumptions incompatible with this
dataset. \textit{Naive Bayes} requires conditional independence of
predictors given class, which fails given the
\texttt{adiposity}--\texttt{obesity} correlation ($r=0.72$) and the
engineered \texttt{age}--\texttt{tobacco} joint effect. \textit{LDA} and
\textit{QDA} assume Gaussian class-conditional distributions
$P(\mathbf{x}\mid Y\!=\!k)\sim\mathcal{N}(\boldsymbol{\mu}_k,\boldsymbol{\Sigma})$;
the binary \texttt{famhist} and skewed variables directly violate this.
\textit{QDA} additionally estimates a full $11\!\times\!11$ covariance
matrix per class: with only 160 CHD-positive training observations this
is severely underdetermined and prone to overfitting.
\textit{Fisher's DA} (FDA) for a binary outcome reduces to a single linear
discriminant direction mathematically equivalent to LDA under equal-covariance
assumptions, and is primarily a supervised dimensionality reduction
tool --- redundant when only 11 features are present and the objective is
risk prediction, not projection.

\textbf{Why the chosen methods are appropriate.}
Ridge logistic regression is the discriminative counterpart to LDA: it
produces an equivalent linear decision boundary without requiring
class-conditional normality, making it strictly more robust when
distributional assumptions are violated \cite{hassan2022effectively}.
The L2 penalty directly addresses the multicollinearity identified in
EDA without zeroing any predictor. The five alternative classifiers span
bagging (Random Forest), boosting (Gradient Boosting), kernel-based
separation (SVM with RBF kernel), instance-based reasoning (KNN), and
an interpretable single-tree baseline (Decision Tree) --- none of which
impose distributional assumptions on the features. This breadth ensures
the partial non-linear class separation visible in the PCA projection
(Figure~\ref{fig:pca}) is captured without assuming Gaussianity,
conditional independence, or equal class covariances.

% =============================================================================
% References
% =============================================================================
\newpage
\bibliographystyle{plainnat}
\bibliography{references}

% =============================================================================
% Appendix
% =============================================================================
\newpage
\begin{appendices}

\section{Code Replication Instructions}

\noindent\textbf{Source code:}\quad
\url{https://github.com/manpreet-sangha/predict_coronary_heart_disease}

\noindent\textbf{Live dashboard:}\quad
\url{https://predict-coronary-heart-disease.streamlit.app/}

\noindent\textbf{Requirements:} Python~$\geq$\,3.10,
\texttt{pandas\,$\geq$\,2.0}, \texttt{scikit-learn\,$\geq$\,1.4},
\texttt{statsmodels\,$\geq$\,0.14}, \texttt{streamlit\,$\geq$\,1.35},
\texttt{plotly\,$\geq$\,5.20}.

\subsection*{Step 1 — Clone the repository}

\noindent\textit{Windows (Command Prompt or PowerShell):}
\begin{lstlisting}
git clone https://github.com/manpreet-sangha/predict_coronary_heart_disease.git
cd predict_coronary_heart_disease
\end{lstlisting}

\noindent\textit{macOS / Linux (Terminal):}
\begin{lstlisting}
git clone https://github.com/manpreet-sangha/predict_coronary_heart_disease.git
cd predict_coronary_heart_disease
\end{lstlisting}

\subsection*{Step 2 — Create and activate a virtual environment}

\noindent\textit{Windows:}
\begin{lstlisting}
python -m venv .venv
.venv\Scripts\activate
\end{lstlisting}

\noindent\textit{macOS / Linux:}
\begin{lstlisting}
python3 -m venv .venv
source .venv/bin/activate
\end{lstlisting}

\subsection*{Step 3 — Install dependencies}
\begin{lstlisting}
pip install -r requirements.txt
\end{lstlisting}

\subsection*{Step 4 — Run the full analysis pipeline}
\begin{lstlisting}
python chd_main.py
\end{lstlisting}
\noindent Outputs (figures, CSVs) are saved to
\texttt{exploratory\_data\_analysis/eda\_output/},
\texttt{logistic\_regression\_ridge\_penalty/lrrp\_output/}, and
\texttt{other\_classifiers/oc\_output/}.

\subsection*{Step 5 — Launch the interactive dashboard}
\begin{lstlisting}
streamlit run streamlit_app/app.py
\end{lstlisting}
\noindent The dashboard opens automatically at \texttt{http://localhost:8501}.
Upload \texttt{input\_data/heart-disease.csv} to explore the results interactively.

\end{appendices}

\end{document}
