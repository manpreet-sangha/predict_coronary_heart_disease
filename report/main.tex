% ============================================================
% main.tex — Coronary Heart Disease Prediction Report
% Individual Coursework — Machine Learning
%
% Requirements:
%   - Max 1000 words, 5 pages
%   - Font size >= 12pt, margins >= 1 inch, 1.5 line spacing
%   - Compiled from the report/ directory:
%       pdflatex main.tex
%       bibtex main
%       pdflatex main.tex (x2)
% ============================================================

\documentclass[12pt,a4paper]{article}

% ── Page layout ───────────────────────────────────────────────────────────────
\usepackage[top=1in, bottom=1in, left=1in, right=1in]{geometry}

% ── Line spacing ──────────────────────────────────────────────────────────────
\usepackage{setspace}
\onehalfspacing
\usepackage{amsmath}

% ── PDF inclusion (submission form) ───────────────────────────────────────────
\usepackage{pdfpages}

% ── Typography & encoding ─────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{microtype}

% ── Tables & figures ──────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\usepackage{caption}
\usepackage{array}          % extended column formatting
\usepackage{multirow}       % multirow cells in tables
\usepackage{wrapfig}        % text wraps around figures/tables
\usepackage{placeins} % use \FloatBarrier explicitly where needed
\usepackage{rotating}       % sideways captions

% ── Code listings ─────────────────────────────────────────────────────────────
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\footnotesize,
  backgroundcolor=\color[gray]{0.95},
  frame=single,
  framerule=0.4pt,
  rulecolor=\color[gray]{0.6},
  breaklines=true,
  breakatwhitespace=true,
  xleftmargin=6pt,
  xrightmargin=6pt,
  aboveskip=4pt,
  belowskip=4pt,
}
\usepackage{xcolor}

% ── Float placement defaults (prevents overflow past page bottom) ─────────────
\renewcommand{\topfraction}{0.85}       % up to 85 % of page may be top floats
\renewcommand{\bottomfraction}{0.70}    % up to 70 % of page may be bottom floats
\renewcommand{\textfraction}{0.10}      % at least 10 % of page must be text
\renewcommand{\floatpagefraction}{0.85} % float-only page must be >= 85 % full
\setlength{\floatsep}{8pt plus 2pt minus 2pt}
\setlength{\textfloatsep}{10pt plus 2pt minus 4pt}
\setlength{\intextsep}{8pt plus 2pt minus 2pt}

% ── Hyperlinks ────────────────────────────────────────────────────────────────
\usepackage[colorlinks=true, linkcolor=black, citecolor=blue, urlcolor=blue]{hyperref}

% ── References ────────────────────────────────────────────────────────────────
\usepackage[numbers,sort&compress]{natbib}
\usepackage{doi}            % clickable DOI hyperlinks

% ── ToC depth ─────────────────────────────────────────────────────────────────
\setcounter{tocdepth}{1}    % sections only — saves space

% ── Appendix ──────────────────────────────────────────────────────────────────
\usepackage[title]{appendix}

% =============================================================================
\begin{document}

% ── Page 1: Coursework Submission Form ───────────────────────────────────────
\includepdf[pages=1]{Group Coursework Submission Form.pdf}

% ── Table of Contents ─────────────────────────────────────────────────────────
\pagenumbering{gobble}
\tableofcontents
\newpage
\pagenumbering{arabic}
\setcounter{page}{1}

% =============================================================================
\section{Exploratory Data Analysis}
\label{sec:eda}
% =============================================================================

The dataset (\texttt{heart-disease.csv}, nine features, one binary label \texttt{chd}
(1\,=\,CHD present), no missing values) contains 462 male patients from the
Western Cape, South Africa.

% ── Table 1 (left) + Figure 1 (right): side by side ──────────────────────────
\vspace{4pt}
\noindent
\begin{minipage}[t]{0.38\linewidth}
\vspace{0pt}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize Descriptive statistics.}
\label{tab:desc}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lrrrr@{}}
\toprule
Feature & Mean & Std & Min & Max \\
\midrule
sbp       & 138.3 & 20.5 & 101 & 218 \\
tobacco   &   3.6 &  4.6 &   0 &  31 \\
ldl       &   4.7 &  2.1 &   1 &  15 \\
adiposity &  25.4 &  7.8 &   7 &  42 \\
famhist   &   0.4 &  0.5 &   0 &   1 \\
typea     &  53.1 &  9.8 &  13 &  78 \\
obesity   &  26.0 &  4.2 &  15 &  47 \\
alcohol   &  17.0 & 24.5 &   0 & 147 \\
age       &  42.8 & 14.6 &  15 &  64 \\
\bottomrule
\end{tabular}}
\end{minipage}\hfill
\begin{minipage}[t]{0.55\linewidth}
\vspace{0pt}
\centering
\includegraphics[width=\linewidth]{../exploratory_data_analysis/eda_output/fig_correlation_heatmap.png}
\end{minipage}%
\begin{minipage}[t]{0.04\linewidth}
\vspace{12pt}
\rotatebox{90}{\footnotesize\textbf{Fig.\,\ref{fig:corr}} Pearson correlation matrix.}
\refstepcounter{figure}\label{fig:corr}
\end{minipage}

\vspace{6pt}
Table~\ref{tab:desc} shows that four features are right-skewed
\cite{rimal2025comparative} i.e.,
\texttt{alcohol} ($\gamma_{1}=2.31$), \texttt{tobacco} ($\gamma_{1}=2.08$),
\texttt{ldl} ($\gamma_{1}=1.31$), and \texttt{sbp} ($\gamma_{1}=1.18$) all
exceed the $|\gamma_{1}|>1$ threshold, which suggests a log-transformation
\cite{feng2014log} could help the model.

Of the 462 patients, 160 (34.6\%) have CHD and 302 (65.4\%) do not, resulting in a
class imbalance ratio of 1.89:1. Thus, instead of just using accuracy, I use AUC and F1-score
as the main evaluation metrics \cite{rehman2025predicting, banerjee2025systematic, dutta2019efficient, borges2025logistic}.
In the Pearson correlation heatmap (Figure~\ref{fig:corr}), the feature most linearly associated with CHD is \texttt{age}
($r=0.37$), followed by \texttt{tobacco} ($r=0.30$), \texttt{famhist} ($r=0.27$), and \texttt{ldl}
($r=0.26$).

On the other hand, \texttt{alcohol} ($r=0.06$) and \texttt{obesity} ($r=0.10$)
show little linear association with CHD. I also noticed a fairly strong
correlation between \texttt{adiposity} and \texttt{obesity} ($r=0.72$), which
means these two features carry overlapping information. Multicollinearity is one of the reasons 
to choose ridge regularisation in Section~\ref{sec:lrrp} \cite{hassan2022effectively, ogunpola2024machine}.

Figure~\ref{fig:logtr} shows the effect of applying a
log\textsubscript{1p}-transformation to the four skewed features.
In each case the skewness drops well below the $|\gamma_1|>1$ threshold:
\texttt{sbp} falls from 1.18 to 0.73, \texttt{tobacco} from 2.08 to 0.25,
\texttt{ldl} from 1.31 to 0.16, and \texttt{alcohol} from 2.31 to 0.03.
After transformation the distributions look much closer to a bell curve
(blue vs.\ red in Figure~\ref{fig:logtr}), which helps models that assume
roughly symmetric inputs e.g. logistic regression and the
generative classifiers (LDA, QDA, Gaussian NB) used in this report.

To clearly demonstrate feature importance, I ranked them using
mutual information (MI) and the ANOVA F-test \cite{elsofany2024proposed, ullah2024machine}. 
I used two measures above as a single metric could be biased.
Table~\ref{tab:fi} shows the normalised scores side by side.
\texttt{age} comes top under both methods, while \texttt{obesity} and
\texttt{typea} score lowest. These rankings influenced which features to
include in the ridge logistic regression model in Section~\ref{sec:lrrp}.

% ── Distribution analysis + log-transform ────────────────────────────────────
\begin{figure}[t]
\centering
\includegraphics[width=\textwidth]{../exploratory_data_analysis/eda_output/fig_log_transformed_features.png}
\caption{\footnotesize Log\textsubscript{1p}-transformation of the
four dynamically identified skewed features.}
\label{fig:logtr}
\end{figure}

% ── Table 2 wraptable + PCA (table on right, PCA text wraps left and below) ──
\begin{wraptable}{r}{0.36\linewidth}
\vspace{-8pt}
\captionsetup{width=\linewidth}
\centering
\caption{\footnotesize Feature importance.}
\label{tab:fi}
\vspace{1pt}
{\scriptsize
\renewcommand{\arraystretch}{0.9}
\begin{tabular}{@{}lrrr@{}}
\toprule
Feature    & MI   & ANOVA & Mean \\
\midrule
age        & 1.00 & 1.00  & 1.00 \\
tobacco    & 0.39 & 0.60  & 0.50 \\
famhist    & 0.43 & 0.48  & 0.46 \\
sbp        & 0.34 & 0.22  & 0.28 \\
ldl        & 0.00 & 0.45  & 0.22 \\
adiposity  & 0.00 & 0.41  & 0.21 \\
alcohol    & 0.16 & 0.00  & 0.08 \\
typea      & 0.07 & 0.04  & 0.06 \\
obesity    & 0.00 & 0.04  & 0.02 \\
\bottomrule
\end{tabular}}
\vspace{-6pt}
\end{wraptable}

To determine the underlying structure, I ran PCA on the standardised features
\cite{banerjee2025systematic}. PC1 captures 32.1\%
of the variance and depends on \texttt{adiposity} (0.52),
\texttt{age} (0.46), and \texttt{obesity} (0.40). 
PC2 picks up lifestyle factors, driven by
\texttt{alcohol} (0.54) and \texttt{tobacco} (0.46). Seven features explain 90\% cumulative variance, 
which shows the data cannot be easily compressed into just two or three dimensions(appropriate reference here). The 2-D
scatter plot (Figure~\ref{fig:pca}) shows a lot of overlap between CHD
and non-CHD patients, meaning a straight (appropriate reference here) line cannot cleanly separate
the two groups. The heatmap (Figure~\ref{fig:pcaload}) shows which features
matter most for each principal component.

% =============================================================================
\section{Logistic Regression with Ridge Penalty}
\label{sec:lrrp}
% =============================================================================

% ── PCA figures (float to next available position) ───────────────────────────
\begin{figure}[htbp]
\centering
\begin{minipage}{0.48\linewidth}
\centering
\includegraphics[width=\linewidth]{../exploratory_data_analysis/eda_output/fig_pca_scatter.png}
\vspace{-6pt}
\caption{\footnotesize PCA 2-D projection by CHD class.}
\label{fig:pca}
\end{minipage}\hfill
\begin{minipage}{0.48\linewidth}
\centering
\includegraphics[width=\linewidth]{../exploratory_data_analysis/eda_output/fig_pca_loadings_heatmap.png}
\vspace{-6pt}
\caption{\footnotesize PCA loadings (PC1 \& PC2).}
\label{fig:pcaload}
\end{minipage}
\end{figure}

Logistic regression with an L2 (ridge) penalty
\cite{borges2025logistic} is chosen as the primary classifier, as instructed. 
Ridge penalty is preferred over lasso \cite{hassan2022effectively}
because lasso can force some coefficients to exactly zero, effectively
dropping features from the model. Ridge, on the other hand, shrinks all
coefficients towards zero but never removes any completely. Ridge also handles correlated features well by shrinking
them together, which is important given the multicollinearity observed between
\texttt{adiposity} and \texttt{obesity} ($r=0.72$, Section~\ref{sec:eda}).

\textbf{Feature engineering.}
Two interaction terms were added:
(i)~\texttt{age\_tobacco} $=$ age $\times$ tobacco --- captures how
smoking risk compounds with longer exposure \cite{rehman2025predicting};
(ii)~\texttt{age\_famhist} $=$ age $\times$ famhist --- family history
risk amplifies with age \cite{patel2023famhist}.

\textbf{Preprocessing.}
\texttt{famhist} encoded as 0/1; four skewed features
log\,\textsubscript{1p}-transformed; all features standardised
\cite{hassan2022effectively}. $C$ selected by 5-fold CV (AUC-ROC) over
$\{0.001,\ldots,100\}$. Two models are fitted:
(i)~unpenalised MLE for inference (Table~\ref{tab:inference});
(ii)~ridge ($C\!=\!0.01$) for prediction (Table~\ref{tab:coef}).

% ── Table 3: right-aligned wraptable, text flows left and below ───────────────
\begin{wraptable}{r}{0.52\linewidth}
\vspace{-6pt}
\captionsetup{width=\linewidth}
\centering
\caption{\scriptsize Unpenalised LR inference ($n\!=\!369$).
$^{*}p<0.05$; $^{**}p<0.01$.}
\label{tab:inference}
\vspace{1pt}
{\scriptsize
\renewcommand{\arraystretch}{0.85}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Feature & $\hat{\beta}_{\text{LR}}$ & SE & $z$ & $p$ \\
\midrule
Intercept    & $-$0.851 & 0.138 & $-$6.19 & $<$0.001$^{**}$ \\
sbp          &    0.136 & 0.135 &    1.01 & 0.312 \\
tobacco      &    0.307 & 0.261 &    1.18 & 0.239 \\
ldl          &    0.420 & 0.148 &    2.84 & 0.005$^{**}$ \\
adiposity    & $-$0.157 & 0.268 & $-$0.59 & 0.558 \\
famhist      & $-$0.357 & 0.527 & $-$0.68 & 0.498 \\
typea        &    0.381 & 0.135 &    2.82 & 0.005$^{**}$ \\
obesity      &    0.017 & 0.211 &    0.08 & 0.938 \\
alcohol      & $-$0.050 & 0.131 & $-$0.38 & 0.704 \\
age          &    0.513 & 0.236 &    2.17 & 0.030$^{*}$ \\
age\_tobacco &    0.088 & 0.243 &    0.36 & 0.718 \\
age\_famhist &    0.825 & 0.535 &    1.54 & 0.123 \\
\bottomrule
\end{tabular}}
\vspace{-6pt}
\end{wraptable}

Table~\ref{tab:inference} shows \texttt{ldl} ($p=0.005$), \texttt{typea}
($p=0.005$), and \texttt{age} ($p=0.030$) are significant under MLE.
Ridge ($C\!=\!0.01$) shrinks \texttt{obesity} and \texttt{alcohol} near
zero, consistent with their non-significance.
Figure~\ref{fig:shrinkage} shows every unpenalised estimate (blue) is
pulled toward zero by the ridge penalty (red), with the largest
reductions on \texttt{age\_famhist} and \texttt{age}. The derived
features improve recall by 12.5 percentage points relative to the
original nine features. On the held-out test set ($n=93$, 20\,\%) the
model achieves AUC\,=\,0.820, F1\,=\,0.610, and accuracy\,=\,0.753
(Table~\ref{tab:perf}). CHD recall is 0.563 versus No-CHD recall of
0.871, a consequence of the 1.89:1 class imbalance
\cite{rehman2025predicting, ogunpola2024machine}.

% ── Table 4: ridge coefficients ──────────────────────────────────────────────
\begin{wraptable}{r}{0.30\linewidth}
\vspace{-8pt}
\captionsetup{width=\linewidth}
\centering
\caption{\scriptsize Ridge coefficients ($C\!=\!0.01$).}
\label{tab:coef}
\vspace{1pt}
{\scriptsize
\renewcommand{\arraystretch}{0.85}
\begin{tabular}{@{}lr@{}}
\toprule
Feature & $\hat{\beta}_{\text{ridge}}$ \\
\midrule
age          &  0.212 \\
age\_famhist &  0.189 \\
ldl          &  0.174 \\
tobacco      &  0.150 \\
famhist      &  0.130 \\
age\_tobacco &  0.125 \\
typea        &  0.122 \\
sbp          &  0.079 \\
adiposity    &  0.073 \\
alcohol      & $-$0.015 \\
obesity      &  0.013 \\
\bottomrule
\end{tabular}}
\vspace{-6pt}
\end{wraptable}

% ── Table 5 (left) + Figure 5 (right): side by side ──────────────────────────
\vspace{4pt}
\noindent
\begin{minipage}[t]{0.46\linewidth}
\vspace{0pt}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\scriptsize Test-set performance of ridge LR.}
\label{tab:perf}
\vspace{1pt}
{\scriptsize
\renewcommand{\arraystretch}{0.85}
\begin{tabular}{@{}lrrrr@{}}
\toprule
Model & AUC & F1 & Prec. & Rec. \\
\midrule
Ridge LR & 0.820 & 0.610 & 0.667 & 0.563 \\
\bottomrule
\end{tabular}}
\end{minipage}\hfill
\begin{minipage}[t]{0.50\linewidth}
\vspace{0pt}
\centering
\captionsetup{width=\linewidth}
\includegraphics[width=\linewidth]{../logistic_regression_ridge_penalty/lrrp_output/fig_lrrp_coefficient_shrinkage.png}
\vspace{-4pt}
\captionof{figure}{\scriptsize Shrinkage: $\hat{\beta}_{\text{LR}}$ vs.\ $\hat{\beta}_{\text{ridge}}$.}
\label{fig:shrinkage}
\end{minipage}

\vspace{6pt}
Figure~\ref{fig:shap} shows a SHAP summary plot for the ridge model.
Each dot represents one test-set patient; the horizontal position shows
how much that feature pushed the prediction towards (right) or away from
(left) CHD. The colour indicates the feature value (red\,=\,high,
blue\,=\,low). \texttt{age} and \texttt{age\_famhist} have the widest
spread, confirming they are the strongest drivers of individual
predictions --- consistent with the ridge coefficients and EDA rankings.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.75\linewidth]{../logistic_regression_ridge_penalty/lrrp_output/fig_lrrp_shap_summary.png}
\caption{\footnotesize SHAP summary plot for the ridge logistic regression
($C\!=\!0.01$). Each dot is one test-set patient; colour encodes feature
value (red\,=\,high, blue\,=\,low).}
\label{fig:shap}
\end{figure}

% =============================================================================
\section{Alternative Classifiers}
\label{sec:oc}
% =============================================================================
Other classifiers such as discriminative models, tree-based ensembles, 
and generative classifiers are evaluated in this section. Same preprocessed data is utilized
as in Section~\ref{sec:lrrp}: \texttt{famhist} encoded, four skewed features 
log\,\textsubscript{1p}-transformed, all features standardised.

All classifiers were screened using stratified 5-fold cross-validation on AUC-ROC
(Table~\ref{tab:final}), with the same 11 features (including
\texttt{age\_tobacco} and \texttt{age\_famhist}) as Section~\ref{sec:lrrp}.
Gaussian NB came out on top in CV (AUC\,=\,0.765), followed by LDA (0.751)
and QDA (0.744). Among tree-based models, Extra Trees (0.730) and Random
Forest (0.718) did best. I then tuned the top CV performer (Gaussian NB)
using GridSearchCV; the best \texttt{var\_smoothing} was $10^{-12}$
(tuned CV-AUC\,=\,0.765). On the test set, however, QDA achieved the
highest AUC (0.828) and F1 (0.694), showing that allowing each class to
have its own covariance structure helps after log-transformation.

% ── ROC curves: Ridge LR vs Random Forest side by side ───────────────────
\begin{figure}[htbp]
\centering
\begin{minipage}{0.48\linewidth}
\centering
\includegraphics[width=\linewidth]{../logistic_regression_ridge_penalty/lrrp_output/fig_lrrp_roc_curve.png}
\caption{\footnotesize Ridge LR ($C\!=\!0.01$): AUC\,=\,0.820, test set $n=93$.}
\label{fig:roc}
\end{minipage}\hfill
\begin{minipage}{0.48\linewidth}
\centering
\includegraphics[width=\linewidth]{../other_classifiers/oc_output/fig_oc_roc_curve.png}
\caption{\footnotesize Gaussian NB (best CV classifier): AUC\,=\,0.811, test set $n=93$.}
\label{fig:oc_roc}
\end{minipage}
\end{figure}
The ROC curves (Figures~\ref{fig:roc} and~\ref{fig:oc_roc}) show that
all models sit well above the diagonal (random guessing) line.
Table~\ref{tab:final} lists results for every classifier on the test set
($n=93$, 20\,\%). QDA gets the highest test AUC (0.828) and F1 (0.694),
just ahead of LDA (0.821) and Ridge LR (0.820). The log-transformation applied earlier 
improved the performance of the generative classifiers (QDA, LDA, Gaussian NB), since they
assume features are normally distributed, making those
assumptions more reasonable. The feature importances from the tree-based
models agree with what I found in EDA: \texttt{age},
\texttt{age\_famhist}, and \texttt{ldl} are the most important predictors
\cite{rehman2025predicting, ogunpola2024machine}.

% ── Confusion matrices: Ridge LR vs Random Forest side by side ────────────
\vspace{2pt}
\noindent
\begin{minipage}[t]{0.46\linewidth}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize Ridge LR: confusion matrix (threshold\,=\,0.5).
Recall — No\,CHD\,85\%, CHD\,56\%.}
\label{tab:cm}
\vspace{2pt}
{\footnotesize
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{@{}llrr@{}}
& & \multicolumn{2}{c}{\textit{Predicted label}} \\
\cmidrule(l){3-4}
& & No\,CHD & CHD \\
\midrule
\multirow{2}{*}{\textit{True Label}} & No\,CHD & \textbf{52} & 9  \\
                                     & CHD     & 14          & \textbf{18} \\
\bottomrule
\end{tabular}}
\end{minipage}\hfill
\begin{minipage}[t]{0.46\linewidth}
\centering
\captionsetup{width=\linewidth}
\captionof{table}{\footnotesize Gaussian NB: confusion matrix (threshold\,=\,0.5).
Recall — No\,CHD\,70\%, CHD\,75\%.}
\label{tab:oc_cm}
\vspace{2pt}
{\footnotesize
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{@{}llrr@{}}
& & \multicolumn{2}{c}{\textit{Predicted label}} \\
\cmidrule(l){3-4}
& & No\,CHD & CHD \\
\midrule
\multirow{2}{*}{\textit{True Label}} & No\,CHD & \textbf{43} & 18 \\
                                     & CHD     & 8           & \textbf{24} \\
\bottomrule
\end{tabular}}
\end{minipage}

\vspace{2pt}
\textbf{Clinical relevance of recall.}
In CHD prediction, sensitivity (recall $= \text{TP}/(\text{TP}+\text{FN})$)
is the most clinically important metric i.e., missing a patient who truly has
heart disease (false negative) risks delayed treatment and potentially a
fatal cardiac event, whereas a false positive leads only to further
diagnostic testing. Ridge LR achieves the highest precision (0.667) but
its CHD recall is only 0.563 i.e., it misses 44\% of CHD patients, which is
clinically unacceptable for a screening tool. Gaussian NB flips this
trade-off: recall rises to 0.750 (catches 75\% of CHD cases) at the cost
of lower precision (0.571), producing more false alarms but fewer missed
diagnoses. This may be acceptable in a screening context.
QDA achieves the best recall (0.781) correctly identifying 78\% of CHD
patients while maintaining the highest overall F1 (0.694) and accuracy
(0.763), making it the most clinically appropriate classifier in this
evaluation.
\vspace{4pt}
\begin{table}[htbp]
\centering
\captionsetup{width=\linewidth}
\caption{\footnotesize Classifier performance sorted by test AUC-ROC. CV columns report 5-fold stratified means.}
\label{tab:final}
\vspace{2pt}
{\footnotesize
\begin{tabular}{@{}lrrr|rrrrr@{}}
\toprule
Classifier & CV AUC & CV F1 & CV Acc & Test AUC & Test F1 & Test Acc & Test Prec & Test Rec \\
\midrule
QDA               & 0.744 & 0.568 & 0.707 & \textbf{0.828} & \textbf{0.694} & \textbf{0.763} & 0.625 & \textbf{0.781} \\
LDA               & 0.751 & 0.534 & 0.713 & 0.821 & 0.630 & 0.710 & 0.561 & 0.719 \\
\shortstack[l]{Ridge LR\\($C\!=\!0.01$)} & 0.764 & 0.458 & 0.732 & 0.820 & 0.610 & 0.753 & \textbf{0.667} & 0.562 \\[4pt]
\shortstack[l]{Gaussian\\NB} & \textbf{0.765} & \textbf{0.591} & 0.686 & 0.811 & 0.649 & 0.720 & 0.571 & 0.750 \\[4pt]
AdaBoost          & 0.717 & 0.518 & 0.694 & 0.799 & 0.576 & 0.731 & 0.630 & 0.531 \\
\shortstack[l]{Extra\\Trees} & 0.730 & 0.478 & 0.699 & 0.795 & 0.567 & 0.720 & 0.607 & 0.531 \\[4pt]
\shortstack[l]{Decision\\Tree} & 0.618 & 0.504 & 0.642 & 0.795 & 0.536 & 0.720 & 0.625 & 0.469 \\[4pt]
\shortstack[l]{Random\\Forest} & 0.718 & 0.485 & 0.694 & 0.788 & 0.571 & 0.742 & \textbf{0.667} & 0.500 \\[4pt]
Bagging           & 0.699 & 0.457 & 0.686 & 0.770 & 0.548 & 0.699 & 0.567 & 0.531 \\
\shortstack[l]{Gradient\\Boosting} & 0.699 & 0.483 & 0.680 & 0.766 & 0.590 & 0.731 & 0.621 & 0.562 \\[4pt]
SVM (RBF)         & 0.716 & 0.483 & \textbf{0.713} & 0.811 & --- & 0.656 & --- & --- \\
LightGBM          & 0.708 & 0.540 & 0.702 & 0.724 & 0.537 & 0.667 & 0.514 & 0.562 \\
KNN               & 0.659 & 0.423 & 0.667 & 0.714 & 0.433 & 0.634 & 0.464 & 0.406 \\
\bottomrule
\end{tabular}}
\end{table}
\newline
\textbf{Stability across repeated splits.}
A single train/test split can yield optimistic or pessimistic estimates
depending on which patients fall in the test set. Figure~\ref{fig:repeated}
shows the distribution of test AUC-ROC across 30 repeated stratified
80/20 splits. Ridge LR has the highest median AUC (0.778), followed by
LDA (0.772) and Gaussian NB (0.764). QDA, despite achieving the best
single-split AUC (0.828), drops to fourth (median 0.749) with wider
spread, indicating greater sensitivity to the specific split. This
confirms that the Ridge LR is the consistently a strong performer for the dataset.

\begin{wrapfigure}{r}{0.46\linewidth}
\vspace{-6pt}
\captionsetup{width=\linewidth}
\centering
\includegraphics[width=\linewidth]{../other_classifiers/oc_output/fig_oc_repeated_splits_boxplot.png}
\caption{\footnotesize Test AUC-ROC across 30 repeated stratified 80/20
splits. Top 3 by median highlighted in red.}
\label{fig:repeated}
\vspace{-8pt}
\end{wrapfigure}

\textbf{Why the chosen methods are appropriate.}
The 12 classifiers in Table~\ref{tab:final} span discriminative models
(Ridge LR, SVM, KNN), tree ensembles (Random Forest, Extra Trees, Gradient
Boosting, AdaBoost, LightGBM, Bagging, Decision Tree), and generative
models (Gaussian NB, LDA, QDA). Log-transformation of skewed features
mitigates the normality concerns, allowing generative classifiers to compete
effectively. Ridge logistic regression addresses the multicollinearity
identified in EDA without zeroing any predictor
\cite{hassan2022effectively}. This breadth ensures the partial non-linear
class separation visible in the PCA projection (Figure~\ref{fig:pca})
is captured across fundamentally different modelling paradigms.

% =============================================================================
% References
% =============================================================================
\newpage
\bibliographystyle{plainnat}
\bibliography{references}

% =============================================================================
% Appendix
% =============================================================================
\newpage
\begin{appendices}

\section{Code Replication Instructions}

\noindent\textbf{Source code:}\quad
\url{https://github.com/manpreet-sangha/predict_coronary_heart_disease}

\noindent\textbf{Live dashboard:}\quad
\url{https://predict-coronary-heart-disease.streamlit.app/}

\noindent\textbf{Requirements:} Python~$\geq$\,3.10,
\texttt{pandas\,$\geq$\,2.0}, \texttt{scikit-learn\,$\geq$\,1.4},
\texttt{statsmodels\,$\geq$\,0.14}, \texttt{streamlit\,$\geq$\,1.35},
\texttt{plotly\,$\geq$\,5.20}.

\subsection*{Step 1 — Clone the repository}

\noindent\textit{Windows (Command Prompt or PowerShell):}
\begin{lstlisting}
git clone https://github.com/manpreet-sangha/predict_coronary_heart_disease.git
cd predict_coronary_heart_disease
\end{lstlisting}

\noindent\textit{macOS / Linux (Terminal):}
\begin{lstlisting}
git clone https://github.com/manpreet-sangha/predict_coronary_heart_disease.git
cd predict_coronary_heart_disease
\end{lstlisting}

\subsection*{Step 2 — Create and activate a virtual environment}

\noindent\textit{Windows:}
\begin{lstlisting}
python -m venv .venv
.venv\Scripts\activate
\end{lstlisting}

\noindent\textit{macOS / Linux:}
\begin{lstlisting}
python3 -m venv .venv
source .venv/bin/activate
\end{lstlisting}

\subsection*{Step 3 — Install dependencies}
\begin{lstlisting}
pip install -r requirements.txt
\end{lstlisting}

\subsection*{Step 4 — Run the full analysis pipeline}
\begin{lstlisting}
python chd_main.py
\end{lstlisting}
\noindent Outputs (figures, CSVs) are saved to
\texttt{exploratory\_data\_analysis/eda\_output/},\newline
\texttt{logistic\_regression\_ridge\_penalty/lrrp\_output/},\newline and
\texttt{other\_classifiers/oc\_output/}.

\subsection*{Step 5 — Launch the interactive dashboard}
\begin{lstlisting}
streamlit run streamlit_app/app.py
\end{lstlisting}
\noindent The dashboard opens automatically at \texttt{http://localhost:8501}.\newline
Upload \texttt{input\_data/heart-disease.csv} to explore the results interactively.

\end{appendices}

\end{document}
